<%- include("layout", {
    title: "Staff Panel",
    body: `
<h1 class="text-3xl font-bold mb-6">Staff Leaderboard</h1>

<table class="w-full bg-gray-800 rounded-xl overflow-hidden">
    <thead class="bg-gray-700">
        <tr>
            <th class="p-4 text-left">Staff Member</th>
            <th class="p-4 text-left">Tickets Claimed</th>
            <th class="p-4 text-left">Tickets Closed</th>
        </tr>
    </thead>
    <tbody>
        <% for (let s of staff) { %>
        <tr class="border-b border-gray-700">
            <td class="p-4"><%= s.name %></td>
            <td class="p-4"><%= s.claimed %></td>
            <td class="p-4"><%= s.closed %></td>
        </tr>
        <% } %>
    </tbody>
</table>
    `
}) %>
